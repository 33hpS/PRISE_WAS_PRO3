#!/usr/bin/env tsx
/**
 * @file auto-commit.ts
 * @description Функциональный автокоммит для мебельной фабрики WASSER
 * Чистая архитектура с иммутабельными данными и типобезопасностью
 */

import { execSync } from 'child_process'

// ===== ФУНКЦИОНАЛЬНЫЕ ТИПЫ ДЛЯ МЕБЕЛЬНОЙ ФАБРИКИ =====

type CommitType = 'auto' | 'feature' | 'fix' | 'refactor' | 'docs' | 'style' | 'feat'
type FurnitureScope = 
  | 'general' 
  | 'components' 
  | 'materials' 
  | 'pricing' 
  | 'pdf' 
  | 'collections' 
  | 'furniture'
  | 'api'

interface CommitConfiguration {
  readonly type: CommitType
  readonly scope: FurnitureScope
  readonly message: string
  readonly dryRun?: boolean
  readonly skipHooks?: boolean
}

interface CommitMetadata {
  readonly timestamp: string
  readonly branch: string
  readonly hasChanges: boolean
  readonly changeCount: number
}

// ===== КОНСТАНТЫ С ИММУТАБЕЛЬНОСТЬЮ =====

const FURNITURE_COMMIT_EMOJIS: Readonly<Record<CommitType, string>> = Object.freeze({
  auto: '🤖',
  feature: '✨',
  feat: '✨',
  fix: '🐛',
  refactor: '♻️',
  docs: '📝',
  style: '💄',
} as const)

const FURNITURE_SCOPE_EMOJIS: Readonly<Record<FurnitureScope, string>> = Object.freeze({
  general: '🔄',
  components: '🧩',
  materials: '🏗️',
  pricing: '💰',
  pdf: '📄',
  collections: '🎨',
  furniture: '🪑',
  api: '🔧',
} as const)

// ===== ЧИСТЫЕ ФУНКЦИИ УТИЛИТЫ =====

/**
 * Безопасное выполнение команды с функциональным подходом
 */
const safeExecute = (command: string): { success: boolean; output: string } => {
  try {
    const output = execSync(command, { encoding: 'utf8', stdio: 'pipe' })
    return { success: true, output: output.trim() }
  } catch (error) {
    return { success: false, output: error instanceof Error ? error.message : 'Unknown error' }
  }
}

/**
 * Функциональная проверка наличия изменений
 */
const analyzeGitChanges = (): CommitMetadata => {
  const branch = safeExecute('git rev-parse --abbrev-ref HEAD').output || 'unknown'
  
  const unstagedResult = safeExecute('git diff --numstat')
  const stagedResult = safeExecute('git diff --staged --numstat')
  
  const changeCount = 
    (unstagedResult.output?.split('\n').filter(Boolean).length || 0) +
    (stagedResult.output?.split('\n').filter(Boolean).length || 0)

  return Object.freeze({
    timestamp: new Date().toISOString(),
    branch,
    hasChanges: changeCount > 0,
    changeCount,
  })
}

/**
 * Генерация семантического сообщения с функциональным подходом
 */
const generateSemanticMessage = (
  config: CommitConfiguration,
  metadata: CommitMetadata
): string => {
  const typeEmoji = FURNITURE_COMMIT_EMOJIS[config.type]
  const scopeEmoji = FURNITURE_SCOPE_EMOJIS[config.scope]
  
  // Функциональная композиция заголовка
  const header = `${typeEmoji} ${scopeEmoji} ${config.type}(${config.scope}): ${config.message}`
  
  const bodyParts = [
    '',
    '🏗️ WASSER Мебельная фабрика - Автоматизация',
    `⏰ Время: ${new Date(metadata.timestamp).toLocaleString('ru-RU', {
      timeZone: 'Asia/Bishkek',
      dateStyle: 'short',
      timeStyle: 'short',
    })}`,
    `🌿 Ветка: ${metadata.branch}`,
    `📊 Изменений: ${metadata.changeCount}`,
    `🎯 Область: ${config.scope}`,
    '',
    '✨ Функциональная архитектура с типобезопасностью',
    '🧩 Модульный подход к компонентам мебельного каталога',
  ].filter(Boolean)

  return [header, ...bodyParts].join('\n')
}

// ===== ОСНОВНЫЕ ФУНКЦИОНАЛЬНЫЕ ОПЕРАЦИИ =====

/**
 * Подготовка файлов к коммиту с функциональным подходом
 */
const prepareFilesForCommit = (): boolean => {
  console.log('📦 Подготовка файлов для коммита...')
  
  const result = safeExecute('git add .')
  if (!result.success) {
    console.error('❌ Не удалось добавить файлы:', result.output)
    return false
  }
  
  console.log('✅ Файлы успешно добавлены в staging area')
  return true
}

/**
 * Создание коммита с типобезопасностью
 */
const createCommit = (message: string): boolean => {
  console.log('💾 Создание типобезопасного коммита...')
  
  // Экранирование специальных символов для безопасности
  const escapedMessage = message.replace(/"/g, '\\"').replace(/`/g, '\\`')
  const result = safeExecute(`git commit -m "${escapedMessage}"`)
  
  if (!result.success) {
    console.error('❌ Ошибка создания коммита:', result.output)
    return false
  }
  
  console.log('✅ Коммит создан успешно')
  return true
}

/**
 * Главная функциональная функция автокоммита
 */
const executeAutoCommit = async (config: CommitConfiguration): Promise<boolean> => {
  console.log('🚀 Запуск функционального автокоммита WASSER...')
  console.log('⚙️ Конфигурация:', {
    тип: config.type,
    область: config.scope,
    сообщение: config.message,
    dryRun: config.dryRun || false,
  })

  // Анализ состояния с функциональным подходом  
  const metadata = analyzeGitChanges()
  
  if (!metadata.hasChanges) {
    console.log('⚠️ Нет изменений для коммита')
    return false
  }

  console.log(`📊 Обнаружено изменений: ${metadata.changeCount}`)

  // Генерация сообщения с иммутабельными данными
  const commitMessage = generateSemanticMessage(config, metadata)

  if (config.dryRun) {
    console.log('🔍 DRY RUN - Предварительный просмотр:')
    console.log('─'.repeat(50))
    console.log(commitMessage)
    console.log('─'.repeat(50))
    return true
  }

  // Функциональная цепочка операций
  const operations = [
    () => prepareFilesForCommit(),
    () => createCommit(commitMessage),
  ] as const

  // Выполнение операций с проверкой успешности
  for (const operation of operations) {
    const success = operation()
    if (!success) {
      console.error('❌ Автокоммит прерван из-за ошибки')
      return false
    }
  }

  console.log('🎉 Функциональный автокоммит WASSER выполнен успешно!')
  console.log(`📝 Тип: ${config.type}(${config.scope})`)
  console.log(`💬 Сообщение: ${config.message}`)

  return true
}

// ===== ТИПОБЕЗОПАСНЫЙ CLI ИНТЕРФЕЙС =====

/**
 * Парсинг аргументов с типобезопасностью
 */
const parseCliArguments = (args: readonly string[]): CommitConfiguration => {
  const [type = 'auto', scope = 'general', message = 'Обновление проекта', ...flags] = args

  return Object.freeze({
    type: type as CommitType,
    scope: scope as FurnitureScope,
    message,
    dryRun: flags.includes('--dry-run'),
    skipHooks: flags.includes('--skip-hooks'),
  })
}

const displayFunctionalHelp = (): void => {
  const helpContent = `
🏗️ WASSER Auto Commit - Функциональный автокоммит с типобезопасностью

📋 Использование:
  tsx scripts/auto-commit.ts [тип] [область] [сообщение] [флаги]

🎯 Типы коммитов для мебельной фабрики:
  auto     🤖 - Автоматическое определение типа
  feature  ✨ - Новые функции (компоненты, материалы)
  fix      🐛 - Исправления (багфиксы, расчеты)
  refactor ♻️  - Рефакторинг кода без изменения API
  docs     📝 - Обновление документации
  style    💄 - Форматирование, стили

🏗️ Области мебельной фабрики:
  components  🧩 - React компоненты каталога мебели
  materials   🏗️ - Система управления материалами  
  pricing     💰 - Алгоритмы ценообразования
  pdf         📄 - Генерация прайс-листов
  collections 🎨 - Мебельные коллекции
  furniture   🪑 - Типы и категории мебели
  api         🔧 - Backend интеграция
  general     🔄 - Общие изменения

🚀 Примеры использования:
  tsx scripts/auto-commit.ts feature components "Новый компонент мебельной карточки"
  tsx scripts/auto-commit.ts fix pricing "Исправление расчета себестоимости" 
  tsx scripts/auto-commit.ts refactor materials "Оптимизация системы материалов"
  tsx scripts/auto-commit.ts auto general "Обновление проекта" --dry-run

🎛️ Флаги:
  --dry-run     Предварительный просмотр без выполнения
  --skip-hooks  Пропуск Git хуков (не рекомендуется)

✨ Особенности функциональной архитектуры:
  • Иммутабельные структуры данных
  • Чистые функции без побочных эффектов  
  • Типобезопасность на всех уровнях
  • Композиция функций для сложной логики
  • Модульность и переиспользование кода
  `

  console.log(helpContent)
}

// ===== ТОЧКА ВХОДА С ОБРАБОТКОЙ ОШИБОК =====

async function main(): Promise<void> {
  const args = process.argv.slice(2)

  // Функциональная обработка help команд
  if (args.some(arg => ['--help', '-h', 'help'].includes(arg))) {
    displayFunctionalHelp()
    return
  }

  try {
    const config = parseCliArguments(args)
    const success = await executeAutoCommit(config)
    process.exit(success ? 0 : 1)
  } catch (error) {
    console.error('❌ Критическая ошибка функционального автокоммита:', error)
    process.exit(1)
  }
}

// Запуск при прямом выполнении
if (import.meta.url === `file://${process.argv[1]}`) {
  main().catch(console.error)
}

// ===== ЭКСПОРТ ДЛЯ МОДУЛЬНОГО ИСПОЛЬЗОВАНИЯ =====
export { executeAutoCommit, generateSemanticMessage, analyzeGitChanges }
export type { CommitConfiguration, CommitMetadata, CommitType, FurnitureScope }