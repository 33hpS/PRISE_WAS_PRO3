# Serve static files as-is (implicit in Pages, left for clarity)
/main.css   /main.css   200
/main.js    /main.js    200
/assets/*   /assets/:splat  200

# SPA fallback (only if the requested file doesn't exist)
# This ensures client-side routing works without breaking static assets.

