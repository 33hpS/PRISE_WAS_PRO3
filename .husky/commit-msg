#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "📝 Проверка сообщения коммита..."

# Проверка conventional commits
npx commitlint --edit "$1" || {
  echo "❌ Некорректное сообщение коммита"
  echo "📋 Используйте формат: type(scope): описание"
  echo "🎯 Примеры для WASSER:"
  echo "   🧩 components: обновление компонентов мебели"
  echo "   🏗️ materials: рефакторинг системы материалов"  
  echo "   💰 pricing: исправление расчетов цен"
  echo "   📄 pdf: улучшение генерации прайс-листов"
  exit 1
}

echo "✅ Сообщение коммита корректно!"
