#!/bin/bash
echo "🔍 Функциональные pre-commit проверки WASSER..."

# 1. Форматирование кода
echo "✨ Проверка форматирования..."
npm run format:check || {
  echo "❌ Код требует форматирования. Запустите: npm run format"
  exit 1
}

# 2. TypeScript типизация (критично)
echo "🏗️ TypeScript архитектурная проверка..."
npm run type-check || {
  echo "❌ Критические ошибки типизации. Исправьте перед коммитом"
  exit 1
}

# 3. ESLint (разрешаем предупреждения)
echo "🔧 ESLint проверка архитектуры..."
npm run lint || {
  echo "⚠️ ESLint предупреждения обнаружены, но коммит разрешен"
}

echo "✅ Функциональные проверки пройдены!"
